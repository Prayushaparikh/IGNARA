# Copy to .env.production before production build (do not commit secrets here)
# Build: npm run build

# Production API — must be HTTPS if the site is HTTPS (avoid mixed content)
VITE_API_URL=https://api.ignaracodex.com

# Legacy / dev API example (HTTP — only for local or pre-HTTPS testing)
# VITE_API_URL=http://54.237.236.205:4000
